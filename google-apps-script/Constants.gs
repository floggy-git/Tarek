/**
 * TAREK RIJSCHOOL — Global Google Sheets configuration.
 */

// Calendar integration is intentionally disabled. Bookings remain fully
// persisted in Google Sheets and no fake calendar IDs are generated.
const CALENDAR_ID = "";
const CALENDAR_SYNC_ENABLED = false;
